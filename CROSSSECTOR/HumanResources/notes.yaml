notesHeader:
  Mapped from the original http://github.com/manfred/mac/schema.json location has been replaced by the location in most of SDM. also removed most of the required attributes and restrictions for allowing different uses of the data model.
notesMiddle:

notesFooter:

notesReadme:
  Mapped from the original http://github.com/manfred/mac/schema.json location has been replaced by the location in most of SDM. also removed most of the required attributes and restrictions for allowing different uses of the data model.